rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for now
    // In production, you'd want to lock this down to authenticated users only
    match /{document=**} {
      allow read, write: if true;
    }
  }
}